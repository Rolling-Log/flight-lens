# SearchIntent

V1 SearchIntent 是对话与表单共同编辑的唯一真相源。

核心字段包括行程类型、出发地、目的地、日期、日期浮动、成人数量、舱位、预算、出发时间、直飞与中转、红眼、行李、附近机场、显式条件、AI 推断及待确认字段。

自然语言解析结果必须回填表单；推断必须带置信度并可修改。AI 不可用时，用户仍可完全通过表单搜索。

确定性校验拒绝返程早于或等于出发日、反向时间窗、过去日期，以及 `directOnly = true` 但 `maxStops > 0` 的冲突输入。预算、时间窗、中转、红眼和行李要求在所有来源标准化后再次统一执行，不能只依赖供应商过滤。

## V1 有限日期浮动

- `flexibleDays = 0`：只检索用户指定日期；
- `flexibleDays = 1..3`：执行三点探测，即基准日期、`-N` 天和 `+N` 天；
- 往返行程保持旅行天数不变，去返日期整体平移；
- 每个 Connector 的实际探测日期写入覆盖报告；
- 部分日期失败必须披露，不能把部分探测表述为完整日期矩阵；
- V2 才提供区间内逐日矩阵和去返日期独立组合。

V1 的 `includeNearbyAirports` 只表示出发地附近机场。目的地附近机场组合属于 V2，界面不得使用含混标签暗示双向支持。
