# 隐私原则

V1 默认匿名使用，仅保存完成搜索和质量审计所需的最小数据。

不收集身份证、护照、支付信息、航司或 OTA 密码及与搜索无关的精确位置。搜索使用随机请求 ID。以后引入账户功能时必须新增隐私评审和删除机制。

默认本地中文解析不会把对话内容发送给外部 AI。仅当服务端明确配置 OpenAI 时，对话文本才用于结构化意图解析，且请求设置为不存储；界面会披露本次使用本地规则还是外部 AI。用户始终可以改用不依赖 AI 的完整表单。
