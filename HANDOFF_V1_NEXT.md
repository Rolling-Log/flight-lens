# 航探 Flight Lens

## 当前状态

项目处于 V1 收尾阶段。V1 的核心验证已经完成：本地网页可以发起真实查询，FlyAI、携程、去哪儿、同程等来源可以通过云端 API 或 Edge Companion 返回真实结果，并统一展示航班、报价、来源状态和购买跳转。

当前版本应称为：**V1 核心能力已实现，最终验收尚未完成**。

尚未完成的 V1 收尾工作：

1. 票面价、机建费、燃油附加费和含税总价的口径需要进一步区分。
2. FlyAI/飞猪搜索结果页可能出现多个代理商报价，系统不能把 API 返回的一个报价称为飞猪最低价。
3. 价格排序需要支持低到高和高到低，并严格遵守用户选择。
4. 综合推荐、最低可核验全价、最低分开购买价需要分成清晰的结论。
5. 分开购买方案需要明确披露两张单程票、规则不一致和库存分别变化的风险。
6. 网页与 Edge Companion 的关系需要在产品界面或文档中说明。

## 最近真实验收事实

- 本地网页运行地址通常为 `http://127.0.0.1:3000`。
- 本地 API 通常为 `http://127.0.0.1:4000`。
- Edge Companion 使用用户自己的 Edge 登录会话读取携程、去哪儿、同程、飞猪页面或结构化响应。
- 真实验收曾达到 5/5 来源成功，也出现过 SerpApi 限流和个别来源页面变化，失败披露机制已经存在。
- 飞猪来源页曾显示同一航班的代理商票面价 `¥1188`、`¥1240` 等，页面明确标注“机票价格（不含税费）”。这不等于系统已经核验了含税最终支付价。
- 结果中曾出现约 `¥1070`、`¥1080` 的分开购买组合；当前产品策略将它们标记为 `split_ticket`，不参与普通单票最低价和综合推荐。

## 下一步执行顺序

### 1. 先读代码和测试

重点文件：

- `packages/contracts/src/index.ts`
- `packages/domain/src/index.ts`
- `packages/connectors/src/flyai.ts`
- `packages/connectors/src/browser-ota.ts`
- `apps/api/src/app.ts`
- `apps/web/app/page.tsx`
- `apps/extension/` 或 Edge Companion 相关目录

### 2. 修复价格口径

- 没有税费明细时，不得显示为含税总价。
- 保留 `priceComponents`、价格状态、抓取时间、卖家和深链接证据。
- 只有完整可核验且有购买落点的报价参与“最低可核验全价”。
- FlyAI 字段语义无法确认时，按“来源展示价，税费待核验”处理，不猜测税费。

### 3. 修复排序

- 增加价格升序/降序状态。
- 用户选择价格排序后，严格按展示价格排序，不要先强制 comparable 优先。
- 同价时再按价格完整性、核验状态、购买落点、耗时和中转次数排序。

### 4. 修复推荐

保留并明确展示三个独立结论：

- 综合推荐；
- 最低可核验全价；
- 最低分开购买价。

分开购买方案不得伪装成普通往返票，但不能隐藏。修复推荐排序中先执行 `comparableFirst`、导致 `recommendedOfferId` 无法置顶的问题。

### 5. 处理飞猪价格差异

- 检查 FlyAI 返回报价与飞猪卖家列表的对应关系。
- 搜索结果页深链接必须标记“需在来源页重新选择”。
- 不能宣称 `¥1240` 是飞猪最低价，除非已读取并核验全部相关卖家报价。
- 页面解析不稳定时，继续失败披露，不使用演示数据。

### 6. 验证

至少运行：

- contracts/domain/connectors/API/web/extension 测试；
- 类型检查；
- 前端和 API 构建；
- 本地真实查询验收。

不要把任何真实密钥提交到 Git。完成本地 V1.1 验收后，再进行 Netlify、Railway、Neon 部署。

## V2 与 V3 边界

- V2：价格历史、趋势判断、价格提醒、定时监控、更多可靠授权来源和云端任务调度。
- V3：账号注册、登录、会话和按用户保存偏好/查询/提醒；匿名数据迁移方案见 `docs/V3_AUTH_OPTIONS.md`。同时再评估更多稳定授权来源，以及 AI 自动搜索策略、价格预测和主动任务。
