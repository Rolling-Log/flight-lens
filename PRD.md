# 航探 Flight Lens 产品需求文档

Status: V1 Development

Product horizon: V1 - V3

Last updated: 2026-08-06

## 1. 产品定义

航探是面向中国境内、从中国出发和到达中国航线的透明航班搜索与决策产品。

它不成为 OTA，也不在结果不足时伪装成“全网最低价”。它通过合法数据来源获取可验证报价，把不同来源标准化为可比较的 Offer，并逐步承担从需求理解、搜索编排、方案比较、持续监控到购买时机建议的完整机票决策工作。

一句话目标：

> 让用户用更少的搜索时间和决策成本，找到并持续确认最适合自己的可购买机票方案。

标准产品对象不是一条航班，而是：

`Offer = Itinerary + Fare + Taxes + Ancillaries + Rules + Seller + Eligibility + Timestamp + Evidence`

## 2. 三阶段总览

| 阶段 | 产品形态 | 必须证明的问题 | 用户最终得到什么 |
|---|---|---|---|
| V1 | 可信航班元搜索 | 能否稳定、合法、透明地比较至少两个实时来源 | 一次可信搜索、统一报价和来源跳转 |
| V2 | 持续航班决策服务 | 能否比一次性搜索更持续地降低用户决策成本 | 更完整的搜索空间、历史比较、收藏和提醒 |
| V3 | 完整 Flight Agent | 能否主动制定策略、持续寻找反例并给出可解释决策 | 个性化搜索计划、买或等建议和持续任务执行 |

版本按能力与验收结果划分，不按开发时间划分。任何阶段的硬门禁未通过时，不得使用该正式版本名称。

## 3. 共同产品原则

1. 价格、库存、行李和退改事实只能来自可审计数据源，不能由模型生成。
2. Connector 负责获取数据；确定性领域代码负责校验、全价、过滤、去重和硬约束；Agent 负责编排和解释。
3. “最低价”只能表示本次成功返回且价格口径可比的来源集合，不宣称未经证明的全网覆盖。
4. 搜索结果页跳转与精确 Offer 跳转必须明确区分。不能精确落到该报价时，只能称为“来源抓取时展示价”。
5. 缺失行李、退改或资格证据时显示未知，不产生“最佳行李”“最宽松退改”等虚假胜者。
6. 商业关系不进入自然排序；未来出现推广结果时必须单独标识并与推荐排序隔离。
7. 数据源必须有授权依据、成本上限和退出机制。禁止自动升级、自动超额和不可控账单，但不把永久零成本作为产品目标。
8. 每次扩展范围前先验证上一阶段的用户价值、数据质量和可持续成本。

## 4. 目标用户与核心场景

### 目标用户

- 需要比较中国国内航线的价格敏感用户；
- 需要比较中国出入境航线、机场和日期的自由行用户；
- 不愿反复打开多个 OTA 或航司 App 的用户；
- 对总价、行李、退改、红眼和中转风险有明确要求的用户。

### 核心任务

1. 固定日期快速找票；
2. 比较不同来源的真实总价与购买条件；
3. 判断换日期、换机场或接受中转是否值得；
4. 持续观察价格并在条件满足时提醒；
5. 获得“现在买、继续等、调整条件”的可解释建议。

## 5. V1：可信航班元搜索

### 5.1 阶段目标

V1 只验证最基础也最关键的假设：系统能否稳定整合至少两个合法实时来源，输出统一且不误导的报价，并把用户带到能够继续核验或购买的来源页面。

V1 的产品不是完整 AI Agent，而是 Flight Agent 所需的可信数据与决策基础层。

### 5.2 用户范围

- 1 位成人；
- 经济舱；
- 单程或往返；
- 固定日期；
- 中国国内、中国出发和中国到达航线；
- 使用 IATA 机场或已支持的城市机场组。

多人、有限灵活日期可以保留为开发能力，但在所有实时来源完成总价口径验证前标记为 Beta，不能进入 V1 核心成功率统计。

### 5.3 必须交付

- 对话输入和结构化表单共同编辑同一份 `SearchIntent`；
- 用户搜索前能看见、确认并修改所有解析条件；
- 至少两个独立、合法、真实生产环境的购买交接 Connector；
- 多来源并行查询、来源级超时、有限重试、缓存和 stale-if-error 披露；
- 航段、往返 leg、承运人、售卖方、币种和时间的统一模型；
- 全价算术、汇率证据、资格价、行李条件和行程结构校验；
- 跨来源 Offer 去重及确定性最低价、最短耗时、最少中转排序；
- 成功、空结果、失败、超时、缓存和不可比报价的用户可见披露；
- 精确 Offer 跳转与来源结果页重新选择的差异化文案；
- 搜索与落地页核价审计；
- 桌面和移动端基础可访问性、安全、限流、Staging 和回滚能力。

### 5.4 推荐能力边界

V1 可以提供“最低全价”“最短耗时”“最少中转”等有直接证据的排序。

“综合推荐”只能作为实验性平衡排序，并公开说明使用的价格、耗时、中转和数据质量因素。缺少用户研究和解释证据前，不称为“最佳方案”。

只有存在明确行李或退改数据时，才显示对应排序和胜出标识。

### 5.5 V1 不做

- 儿童、婴儿、多城市和复杂舱位组合；
- 完整灵活日期矩阵；
- 目的地附近机场自动展开；
- 复杂拆票、自助中转推荐和机场间转移方案；
- 用户账户、收藏、历史价格和自动提醒；
- 价格预测和“值得等待”结论；
- 自动出票、支付或代办退改；
- 酒店、租车、签证和行程攻略。

### 5.6 V1 发布门禁

- 两个购买交接来源完成真实生产查询和授权复核；
- 至少一个来源能提供精确 Offer 交接；仅有结果页重选不能单独通过门禁；
- 核心受控查询成功率不低于 90%；
- 95% 的受控搜索在 25 秒内返回成功结果或明确超时状态；
- 至少完成 20 组覆盖国内、出境、入境、往返、跨日和多机场的受控查询；
- 至少完成 30 个报价的落地页复核，并分别统计精确跳转与结果页重选的价差；
- 搜索结果页跳转不得展示为“可支付总价”；
- 不符合预算、红眼、行李、资格或中转条件的 Offer 不进入主结果；
- 类型检查、Lint、单元测试、生产构建和桌面/移动端 E2E 全部通过；
- Staging 数据库审计、限流、成本硬停止和回滚演练通过；
- 经产品负责人确认后才合并 `main`、创建 `v1.0.0` Tag 和部署 Production。

### 5.7 当前状态

- 双输入、统一契约、编排、排序、审计、Web/API 和自动化测试已基本完成；
- Web 已切换为 Netlify 独立部署、API 已切换为 Railway 长驻服务，新的跨域 Staging 尚待验证；
- SerpApi Google Flights 已完成受控生产查询；
- Skyscanner Connector 已实现，合作申请等待审批；
- 实时购买交接来源为 1/2，因此仍是 V1 开发版；
- SerpApi 结果页重选出现过显著价格变化，必须在正式发布前收紧价格措辞和可比性规则；
- 当前可继续做可信度修复和受控验证，不进入 V2 功能开发。

## 6. V2：持续航班决策服务

### 6.1 进入条件

只有 V1 正式发布、真实用户能够完成来源交接，且数据质量与单次搜索成本可接受后，才启动 V2。

### 6.2 阶段目标

把“一次搜索”升级为“持续帮助用户做选择”。V2 的核心不是增加页面数量，而是扩大有效搜索空间，并让用户能够保存、比较和持续观察候选方案。

### 6.3 必须交付

- 更多经过授权的中国航线来源，包括优先评估航司直营、NDC/GDS 和国内 OTA 合作接口；
- 多成人、儿童以及经济舱/超级经济舱/商务舱；
- 多城市行程；
- 完整 ±3 天日期矩阵和往返日期组合；
- 出发地与目的地附近机场、同城机场和机场转移风险披露；
- 用户可并排比较多个 Offer 的总价、耗时、行李、退改和交接精度；
- 搜索历史、收藏方案和条件模板；
- 首次查询即可展示的外部市场历史，以及独立的本系统查询留存；两者必须标出来源、范围、更新时间和价格口径，不能拼接成一条趋势；
- 价格与条件提醒，包括目标价、指定航班、直飞、行李和时间窗；
- Connector 质量仪表盘：成功率、延迟、空结果率、字段完整度、落地价偏差和成本；
- 全局原子配额、任务调度、提醒去重、退避和失败恢复；
- 用户偏好模型，包括价格敏感度、红眼、中转、机场、行李和退改偏好；
- 每项推荐给出可读理由和主要取舍。

### 6.4 V2 决策能力

- 比较换日期、换机场、增加一次中转带来的节省与时间成本；
- 识别明显不合理的低价、过期价、条件价和自助中转风险；
- 根据用户明确偏好生成个性化排序；
- 用户可以选择“只提醒，不自动改变搜索条件”或允许 Agent 扩大搜索范围；
- 没有足够历史数据时只展示事实趋势，不输出价格预测。
- 当前价格相对同口径历史的五档分位判断：拉完了、NPC、人上人、顶级、夯；横轴从偏贵到更划算，并同时标出当前价、典型区间和历史中位数。
- 五档边界默认采用历史每日最低展示价的 P20/P40/P60/P80；等于边界时进入更有利的一档。少于 5 个观测日、航线或日期不一致、价格口径不一致、分开购买价时不输出等级。

### 6.5 V2 不做

- 自动下单、自动支付或保存支付资料；
- 无保护拆票的默认推荐；
- 没有校准数据的买入时机预测；
- 酒店和完整旅行规划；
- 为扩展规模而提前拆分微服务。

### 6.6 V2 发布门禁

- 核心查询成功率不低于 95%，P95 响应时间不高于 15 秒；
- 至少三个稳定实时来源，其中至少一个对中国国内航线有实质覆盖；
- 精确 Offer 交接覆盖率、落地页价差和售罄率形成持续报表；
- 航班与 Offer 去重抽样准确率不低于 99%；
- 提醒任务成功率不低于 99%，重复提醒率低于 1%；
- 用户能在测试中完成“保存搜索 - 收到变化 - 返回比较 - 前往来源”的闭环；
- 至少一项用户研究证明 V2 相比手动多平台搜索降低了时间或决策成本；
- 成本上限、隐私删除、通知退订和故障恢复通过验收。

## 7. V3：完整 Flight Agent

### 7.1 进入条件

只有 V2 的搜索、历史、提醒和偏好数据形成稳定闭环，并拥有足够样本评估推荐与价格趋势质量后，才启动 V3。

### 7.2 阶段目标

V3 完成机票领域的完整 Agent：用户表达目标后，系统能够制定搜索策略、执行多轮受控搜索、寻找反例、解释取舍、持续监控，并在信息变化时更新建议。

### 7.3 必须交付

- 自然语言与结构化条件的双向持续编辑；
- 面向开放需求的搜索计划，例如“下个月任意周末去东京玩五天”；
- 在用户授权的边界内探索日期、机场、航线、中转、舱位和票种组合；
- 复杂拆票、开放式航程和自助中转方案，但必须计算衔接、签证、行李、误机保护和机场转移风险；
- 基于用户偏好、真实 Offer 和历史数据的个性化推荐；
- 可解释的“现在买、继续等、调整条件”建议；
- 价格预测模型、置信区间、校准报告和失效降级；
- 持续任务：定时搜索、阈值提醒、候选变化、售罄风险和替代方案；
- 主动反例搜索：尝试用邻近日期、机场或不同组合推翻当前推荐；
- 推荐变化记录，说明新数据如何改变结论；
- Agent 行动预算、供应商调用预算、停止条件和人工确认点；
- 完整的审计、隐私、模型评估、安全红队、故障降级和生产 SLO。

### 7.4 Agent 行为规则

- Agent 可以决定“搜什么”，但不能修改用户硬约束；
- Agent 可以解释推荐，但价格、库存和规则必须引用来源证据；
- Agent 不能因为模型不可用而阻断基础表单搜索；
- Agent 每次扩大日期、机场、预算或中转范围前必须获得明确授权；
- 拆票、自助中转、过夜、换机场和签证风险必须显著披露；
- 预测置信度不足时退化为历史事实和规则提示；
- 所有自动任务都必须有预算、频率、到期时间和一键停止能力。

### 7.5 V3 完成标准

- Agent 对核心开放需求能够生成可执行、可修改的搜索计划；
- 计划执行失败、部分覆盖和成本停止都能正确降级；
- 推荐解释能追溯到用户偏好、Offer 字段和来源证据；
- 买入时机模型通过回测与线上校准门禁，持续报告误差而非只报告命中案例；
- 复杂方案风险识别通过人工构造的对抗测试集；
- 真实用户研究证明 Agent 建议在价格、时间或决策成本上优于基准流程；
- 核心服务达到定义的生产 SLO，并完成安全、隐私、无障碍、成本和灾难恢复审查；
- 不依赖单一数据源、单一模型或单一不可替换供应商维持核心能力。

### 7.6 V3 仍不包含

- 航探直接出票、代收款或保存身份证件和支付资料；
- 未经用户最终确认的自动购买；
- 代办退改签和售后客服；
- 酒店、租车、签证、攻略和通用 Travel Agent。

自动出票与支付会引入牌照、资金、身份信息、售后和履约责任，应作为 V3 之后的独立商业与合规项目，而不是 Flight Agent 是否完成的判断标准。

## 8. 跨版本指标

| 指标 | V1 | V2 | V3 |
|---|---:|---:|---:|
| 稳定实时购买交接来源 | 2 | 3+ | 无单点依赖 |
| 核心查询成功率 | >= 90% | >= 95% | 按生产 SLO |
| 搜索 P95 | <= 25 秒 | <= 15 秒 | 按计划类型分级 |
| 搜索形态 | 固定条件 | 扩展条件 + 持续监控 | Agent 多轮计划 |
| 推荐 | 确定性排序 | 偏好化解释 | 个性化决策与反例搜索 |
| 价格历史 | 无 | 外部市场历史 + 独立的本站查询留存 | 校准预测与买入建议 |
| 用户关系 | 匿名单次搜索 | 收藏、历史、提醒 | 持续 Agent 任务 |

核心北极星指标不是搜索次数，而是：

> 用户通过航探完成可信购买交接的比例，以及相比自行多平台搜索节省的时间和决策成本。

## 9. 数据、成本与合规策略

- 优先接入航司直连、NDC/GDS、授权聚合服务和 OTA/元搜索合作接口；
- 浏览器自动化仅用于获准的补充核验，不绕过登录、验证码、访问控制和付费限制；
- 数据源成本采用明确预算、单次搜索成本和月度硬停止管理；
- 免费方案可以用于开发和早期验证，但不能成为正式产品的唯一可持续性假设；
- 搜索、报价和核价证据只保存完成产品目的所需的最小信息；
- 不收集支付信息、身份证件、平台密码或与搜索无关的精确位置；
- 引入账户、通知或模型训练前必须分别完成隐私评审。

## 10. 版本推进规则

1. 当前只执行 V1 发布门禁和可信度修复。
2. V1 未证明双来源与购买交接前，不开发 V2 提醒、历史和账户功能。
3. V2 未形成真实用户与历史数据闭环前，不开发 V3 价格预测和自主规划。
4. 每个版本先在 Staging 完成自动化、真实来源、核价、成本与回滚验证。
5. 只有产品负责人明确确认，才能合并 `main`、创建正式 Tag 和部署 Production。
